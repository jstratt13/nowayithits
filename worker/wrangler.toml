name = "dbp-tracker-worker"
main = "src/index.js"
compatibility_date = "2026-05-01"
workers_dev = true

# KV cache so we don't hammer Basketball-Reference / NBA.com / Rotowire.
# Each scraper has its own TTL; see src/cache.js.
#
# After `wrangler login`, create the namespace with:
#   wrangler kv:namespace create CACHE
# then paste the id below.
[[kv_namespaces]]
binding = "CACHE"
id = "REPLACE_WITH_KV_ID"

[vars]
# Override per-environment via `wrangler secret` if you need to.
USER_AGENT = "dbp-tracker/0.1 (+https://github.com/your-username/dbp-tracker)"
